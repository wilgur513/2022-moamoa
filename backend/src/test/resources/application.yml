spring:
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        show_sql: true
        format_sql: true
logging:
  level:
    org:
      springframework:
        jdbc:
          core: trace
      hibernate:
        type:
          descriptor:
            sql: trace
